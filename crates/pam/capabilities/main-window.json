{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Allow the local pam main window its core webview surface (core:default includes core:event:default, which the pam://event listener needs), the IPC bridge commands, and titlebar dragging (needed by the custom titlebar).",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-daemon-status",
    "allow-admin-call",
    "allow-request-capability",
    "allow-daemon-stop",
    "allow-events-subscribe",
    "allow-read-daemon-log",
    "core:window:allow-start-dragging"
  ]
}
